using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shop : MonoBehaviour
{

  //When shop is spawned
  //Create a timer that when up destroy the Shop
  //Make 8 interactable objects, that when you grab, makes hand component that object if they have enough money
}
